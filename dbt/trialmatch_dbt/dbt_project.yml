name: 'trialmatch_dbt'
version: '1.0.0'
config-version: 2

profile: 'trialmatch'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  trialmatch_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
